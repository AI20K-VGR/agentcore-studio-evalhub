# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
# Env (bí mật — không commit)
.env
.env.*
# uv.lock sống ở repo cha (agentcore-studio-kit), KHÔNG ở submodule này

# Plan của AIE-2 phải track được: comment đóng issue trỏ thẳng vào file này, và kit#74 tính
# "artifact không tìm thấy trong fresh clone" là trừ điểm chứ không phải cộng.
# Allowlist theo PATTERN, không liệt kê từng ngày — allowlist liệt kê tay là đúng cơ chế đã nuốt
# im lặng day-13 và day-14 (file có thật trên đĩa, 404 trên GitHub). Scratch/plan nháp khác vẫn ignore.
docs/plans/*
!docs/plans/day-*-aie2.md
